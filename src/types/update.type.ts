// TODO(Step 3): Replace with proper Tauri update types
// Temporary type definition for UpdateInfo (migrated from electron-updater)
export interface UpdateInfo {
  version?: string
  releaseName?: string
  releaseNotes?: string
  [key: string]: any
}

